# Must point at the backend API (not the Vercel frontend URL)
# Local:
REACT_APP_API_URL=http://localhost:5000/api/v1
# Production (set this in Vercel → Settings → Environment Variables, then Redeploy):
# REACT_APP_API_URL=https://pulse-backend-tkpf.onrender.com/api/v1
